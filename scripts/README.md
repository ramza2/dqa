# Scripts

Operational scripts will be added as implementation progresses.

Planned:
- `deploy.sh`: Docker Compose + external Traefik deployment
- validation helpers where useful

Do not place credentials in scripts.
